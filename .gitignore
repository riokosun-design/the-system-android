.gradle/
build/
local.properties
*.iml
.idea/
.cxx/
app/build/
supabase/.branches
supabase/.temp

# secrets — never commit
gradle.properties
DEPLOYED_LIVE.md

# keystores — RELEASE never committed; DEBUG committed on purpose so every
# cloud build signs identically (OAuth SHA-1 match). Debug certs cannot upload to Play.
*.keystore
!debug.keystore
keystore/*.base64.txt
.kotlin/
